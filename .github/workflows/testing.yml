name: Testing

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: testing-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Linux window manager
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes openbox x11-utils

      - name: Run tests
        run: npm test

      - name: Run Electron end-to-end tests
        run: xvfb-run --auto-servernum --server-args="-screen 0 2560x1440x24" bash scripts/run-electron-with-openbox.sh npm run test:e2e
